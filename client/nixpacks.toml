# Nixpacks configuration for Railway
[variables]
NODE_VERSION = "20"
NODE_ENV = "production"

[phases.setup]
nixPkgs = ["nodejs-20_x", "npm-10_x"]

[phases.build]
cmds = [
  "npm ci --only=production",
  "npm run build"
]

[phases.start]
cmd = "npm start"